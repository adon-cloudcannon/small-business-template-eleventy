{% assign c="c-component-heading" %}

<section class="{{ c }}">
    <h3 class="{{ c }}--eyebrow_heading {{ c }}-{{ text_alignment }}">
        {{ eyebrow_headline }}
    </h3>
    <h2 class="{{ c }}--primary_heading {{ c }}-{{ text_alignment }}">
        {{ primary_heading }}
    </h2>
</section>